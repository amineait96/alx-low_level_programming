#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch = 'a';
	char s = "\n";

	while (ch <= 'z')
	{

	putchar(ch);

	ch++;

	}
	putchar(s);
	return (0);
}
